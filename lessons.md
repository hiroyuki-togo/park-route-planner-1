# Lessons — TDL Route Planner

> このプロジェクトを通じて学んだこと・次に活かしたいこと。
> 2026-07-02 スリム化: 個別事象の全文（背景・経緯・検証ログ）は [lessons_archive.md](lessons_archive.md) へ全文移動。本体は現在も有効な要点ダイジェストのみ維持する。番号は元番号のまま。

## コミュニケーション・同士スタンス（最重要・頻出）

- **#4 / #34**: 東郷さんは GitHub の概念に不慣れ。PR / branch / remote 等の用語だけでなく、UI ボタン（鉛筆アイコン → Codespaces 起動等）も初出で必ず「= 〇〇のこと」と展開する
- **#6 / #13 / #35**: 同士スタンス。設計判断・プランの方針・ルール適用の判断は「勝手に修正」ではなく「推奨 + 理由を提示して東郷さんが選ぶ」。特にユーザー意図を弱める分岐（警告して諦める等）と CLAUDE.md ルールの機械的適用は要事前確認
- **#16**: 仕様質問には「結論先・式は補足」。式ではなく挙動の傾向を先に答える
- **#5**: 「無料枠 MUST」は強い制約。課金リスクのある選択肢は提案時に必ず明示

## 外部依存・検証（このプロジェクト最大の教訓群）

- **#22**: 「動いている」と思い込んでいる機能は、ログ・ファイル名（dummy/sample 混入）・実 fetch で実証してから信用する。UI の成功表示は嘘の可能性が常にある
- **#23 / #24**: OLC 公式 API は WAF が curl/requests を TLS 指紋で完全黙殺 → 第三者集約 Queue-Times.com で解決。第一命題を諦める前に代替手段マトリクス（公式別経路 / 第三者集約 / 半手動 / 諦め）を網羅する
- **#25 / #28**: マスタデータは年次見直し必須。「API に無い = 取れない」と即断せず、クローズ済みか・旧 ID 引き継ぎ配信かを確認。現実世界側のルール変更（制度刷新・建て替え）も能動的に追う。「動いている」と「正しい」は別物
- **#26 / #33**: 修正 commit 前に変更シンボルをリポジトリ全体 grep して波及確認 + 実 fetch で結果数字を確認。クラウドコンテナは UTC 固定で、ローカル（Mac=JST）では絶対再現しない時刻バグがある（`_now_jst()` = `utcnow() + 9h` ヘルパー方式で統一）
- **#31**: 外部 API の境界条件（閉園後は全件 closed 等）が候補プールを枯渇させる沈黙のバグになる。「データを返している = 使える」ではない。境界条件の再現テストを持つ
- **#1 / #2**: スクレイピングは「実 HTML / API を覗く」を最初のタスクに。HTML より内部 JSON API（Network タブで確認）が堅牢

## 設計・実装

- **#12 / #32**: 貪欲法スケジューラでは「候補枯渇」と「idle until next event」を区別する。`visited` と「消化予定（予約枠）」も区別する（`visited | reserved_ids` で候補除外）。「同じアトラクションは 1 回だけ登場」のような cross-step invariant は先に言語化してテスト化する
- **#18**: 役割重複は「片方を削る」より「別モードで分けて両立」。ただし機能成熟後に重複が見えたら統合（分岐削減）方向に倒す判断もある。モード間で意味が違う state は切替時に明示クリア
- **#27**: 永続化レイヤー（localStorage / DB）はマスタ変更を自動追従しない。復元層に「現存マスタとの積集合フィルタ」を置く（`set(saved) & valid_ids`）
- **#29 / #30**: 個人ツールでは後方互換シムは負債（一気切り替え + 旧名全 grep + テスト全 PASS で保証）。Pydantic v2 は `extra="ignore"` 既定で unknown kwarg を silent-drop する（`extra="forbid"` 検討 = PROGRESS K-C）
- **#19**: Streamlit のリセットは `del session_state[key]` では不完全。widget key に `_{reset_token}` suffix を付けて key 自体を変えるのが確実
- **#20**: CSS が効かない時はセレクタを重ねる前に computed style で「見えている現象を作っているのが本当にその要素・プロパティか」を確認
- **#11**: 範囲チェックは緩く + 分布ベース sanity check（エントランス距離の突出等）で補完
- **#14 / #15**: 外部依存 UI は失敗パスから先に試す。silent except は UX デバッグの最大の敵。決定的な dummy 生成スクリプト（seed 固定 / gitignore / README 記載）をペアで作る
- **#10 / #34**: 人力データ入力は「Excel テンプレ + 検証付きインポーター + 異常値ハイライト」の 3 点セット。1 年後の追加運用（14 件拡充）でもそのまま回収できた。Streamlit Cloud で反映されない時は「Reboot app」

## ワークフロー・環境・ドキュメント

- **#7 / #8**: subagent / 3-reviewer は機械的タスクでは過剰（pytest 直接実行で代替可）。プランは書き終えたら placeholder がないか self-review
- **#17**: 実装難度が大きく違う選択肢が並んだら「目的は何か」を先に東郷さんに決めてもらう（データ蓄積 vs 合成シミュの ROI 判断）
- **#21**: 1 ファイルに多論点が同居した未コミット作業は `git apply --cached --unidiff-zero` で index だけに部分パッチ → コミット分割（worktree 不変で auto-classifier も通る）
- **#3**: macOS システム Python は 3.9 系。プロジェクト初期化前に `python3 --version` を確認
- **#9**: 複数ドキュメントが並んだら「どれが正本か」を 1 箇所で宣言する

---

個別事象の全文（#1〜#35、背景・経緯・検証ログ含む）: [lessons_archive.md](lessons_archive.md)
