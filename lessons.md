# Lessons — TDL Route Planner

> このプロジェクトを通じて学んだこと・次に活かしたいこと。
> 50 行を超えたら古い順に整理してスリム化する。

---

## 設計・実装

### 1. スクレイピング対象は事前に「実 HTML を覗く」を最優先タスクに
TDL 公式サイトはクライアントサイドレンダリングで、`curl` した HTML にデータが含まれていなかった（実データは内部 JSON API から JS が取得）。Task 5 の fixture 保存時点で発覚し、仕様書 §4 とプラン Task 6-8 を書き直した。
**学び**：新規スクレイピング対象は **「セレクタ確認 fixture」を最初のタスク**にして、設計詳細を詰める前に「実際にデータが取れる構造か」を検証する。

### 2. JSON API は HTML スクレイピングより堅牢
TDL の `/_/realtime/tdl_attraction.json` は正式名で名前揺れが少なく、DPA / FastPass 状況も別フィールドで取れる。BeautifulSoup の必要性が消えた。
**学び**：HTML スクレイピング前に **ブラウザ開発者ツールの Network タブ** で API 呼び出しを確認するのが筋。

---

## 環境

### 3. macOS のシステム Python は 3.9 系
`pyproject.toml` の `requires-python = ">=3.11"` を満たさず、`brew install python@3.11` の追加インストールが必要だった。Phase 1 で 10 分ほどロス。
**学び**：プロジェクト初期化前に `python3 --version` を確認するステップを Task 1 の最初に組み込む。

---

## コミュニケーション

### 4. 東郷さんは GitHub の概念に不慣れ
PR / branch / CI / remote 等の用語は初出で必ず展開する。
**学び**：用語は省略しない。「これって何？」が来る前に **「= 〇〇のこと」と補足を入れる**。

### 5. 「無料枠 MUST」要件は強い制約
追加課金の可能性がある選択肢（Playwright 等）は提案時に必ず「無料枠との衝突」を明示する。

### 6. 「同士スタンス」を貫く
設計判断で迷ったら両論併記、東郷さんが選ぶ形にする。
「これでいいですか？」だけで終わらせず、**自分の推奨と理由をセットで提示**する。

---

## ワークフロー

### 7. subagent-driven は機械的タスクではコスト過剰
完全プラン通りの実装で、self-review が通っているタスクは spec review を **`pytest` 直接実行で代替**できる。3 reviewer を全タスクで起動するのは無料枠原則と相性悪い。
**学び**：タスクごとに「フル 3-reviewer」か「self-review + 直接検証」かを判断する。

### 8. writing-plans でも self-review は必須
プラン Task 16 で `if False  # 次のタスクで詰める` という dead code を書きかけた。fresh-eyes self-review で発見して inline 修正。
**学び**：プランを書き終えたら **「実装側から見て placeholder がないか」** を必ず再走査する。

---

## ドキュメント運用

### 9. ドキュメントは「正本」と「参考」を明示
TDS → TDL ピボット時、`ディズニープラン.md` を `archive/` に移し、新仕様書（`docs/superpowers/specs/`）を正本に切り替えた。CLAUDE.md / memory.md / PROGRESS.md の各所に「正本はこれ」と明記。
**学び**：複数ドキュメントが並んだら **「どれが最新で正か」を 1 箇所で宣言**する。

---

## 人力タスクとのインターフェース

### 10. 人力データ入力には「形式チェック付きの Excel + 安全なインポーター」をペアで作る
Phase 3 で 31 件の座標を東郷さんに手入力してもらう必要があった。当初は「JSON を直接編集してください」と案内する想定だったが、東郷さんから「Excel に書き出して、埋めたものを共有する形にしてほしい」と提案を受けて切替。結果として：

- **Excel 側**：ID 列を編集禁止にして並べ替え（エリア順）で検索効率を上げ、入力欄は薄黄色で目立たせた
- **インポーター側**：ID で突合（名前変更耐性）、TDL 緯度経度範囲外を検出、空欄は安全スキップ、Excel にあって JSON にない ID を警告

実際、入力された 31 件のうち 1 件（omnibus）が TDL 範囲を超えた誤入力で、**エントランスからの距離チェック**で即座に発見できた（1040m → 他は 100〜400m）。範囲だけでなく分布のスポットチェックも価値あり。

**学び**：人力データ入力を伴うタスクでは「**入力テンプレート + 自動検証付きインポーター + 異常値ハイライト**」の 3 点セットを最初から組む。手入力の正確性を信用しない設計が、結果として東郷さんの作業負担も減らす。

### 12. 貪欲法ルーターでは「候補枯渇」と「未消化の固定ブロック」を分けて考える
Task 16 で実装したルーターに当初バグが残っていた。`_candidate_pool` が空になった時点で即 `break` していたため、まだ消化されていない DPA 固定ブロック（例: 10:30 開始）に到達する前にループが終わり、`dpa_window_missed` 警告だけが出る現象。
**学び**：「やることがない」と「予定はあるが今すぐは動けない」は別物。候補プールが空でも残り `fixed_blocks` があれば、その開始時刻まで `current_time` を早送りして再ループに入るべき。スケジューラ系の貪欲ループでは、**待機 (idle until next event) は break と同じくらい重要なステート**。

### 13. プランの設計判断を実装前に必ず東郷さんに確認する
Task 16 で「must が固定ブロック前に収まらない場合は諦めて警告」というプラン記述があった。実装前に内容を要約報告したところ「他の任意を後回しにしてでも must を入れる」が自然との指摘が入った。プラン通りに実装していたら、`must_visits` の意図（= ユーザーが「絶対乗りたい」と指定したもの）を簡単に弱める実装になっていた。
**学び**：プランは仮の正解。**ロジックの方針が東郷さんの意図に合うかは、実装前のサマリ確認フェーズで毎回問い直す**。特に「警告して諦める」「優先順位を下げる」など、ユーザー意図を弱める分岐は要注意。

### 14. UI のレスポンス確認は「失敗パスから先に試す」
Phase 6 の Streamlit 動作確認で、いきなり「ルート生成」を試してもらった結果、ライブ API レート制限で「取得に失敗しました」だけが出てしまった。`fetch_realtime_wait_times()` が exception を silent swallow していたため、UI でも `streamlit` ターミナルでも原因が出ず、切り分けに `curl` を別途叩く必要があった。
**学び**：外部依存（API・DB・ファイル等）のある UI を作る時は、**最初から「失敗時のメッセージ表示」と「ログ出力」をセットで実装する**。silent except は UX デバッグの最大の敵。実装の早い段階で「失敗パスのトレース性」を担保する。

### 15. 個人ツールでは「ダミーデータ生成スクリプト」が運用を救う
TDL 公式 API がレート制限で応答しないとき、過去スナップショットも空だった。`scripts/generate_dummy_snapshot.py` を作って 21 件分のダミーを生成 → フォールバック経由で UI 動作確認を完走できた。
**学び**：**外部 API に依存する個人ツールには「決定的な fixture 生成スクリプト」を必ずペアで作る**。`random.seed(42)` で再現可能、`.gitignore` で出力先を除外、README に「動かない時はこれを叩け」と明記する 3 点セット。

---

## UX 観察（来園日後の見直しメモ）

### 16. スコア式の説明が技術者向けすぎる
東郷さんから「『必ず乗る』に優先度をつけるとどう並ぶ？」と質問されたとき、`score = priority × EXP_VALUE / cost` の式をそのまま提示してしまった。本来は「priority の差より移動距離の差のほうが効きやすい」のような**挙動の傾向**を先に伝え、必要なら式を補足するのが筋。
**学び**：仕様確認の質問には「**結論先・式は補足**」で答える。式を聞かれているわけではなく、操作の結果がどう変わるかを聞かれている。

---

### 19. Streamlit の widget 内部 state は `del session_state[key]` だけでは消えない
リセット機能を実装したとき、最初は「session_state から widget key を `del` すれば widget が default 値に戻る」と想定していたが、実際は **DPA だけ消えて他は戻らない** という不可解な現象が起きた。原因は number_input の自動 key の値が前回値を保持していたこと + Streamlit が widget の identity を保とうとして内部 state を残すこと。

**確実な解は「widget key 自体を変える」**。session_state に `reset_token: int` を持って、リセットのたびに +1 し、全 widget の key 末尾に `_{token}` を付ける。Streamlit から見ると別 widget なので、内部キャッシュからの値復帰が一切起きない。

**学び**：Streamlit で「リセットボタン」を作るときは、最初から `key=f"name_{reset_token}"` パターンを採用する。`del session_state[key]` で消すアプローチは「見える widget は消えるが、見えない widget の値は残る」というバグの温床。number_input 等の自動 key にも明示 key を振る必要がある（さもないと自動 key 配下の値が残り、表示件数が前回値で復活する）。

---

### 18. 役割重複の解消は「機能を削る」より「別モードを足す」方が筋がいい
シミュレーションモードを追加した直後、当日モードの「9:00 開園想定固定」が **シミュと機能重複している** ことが判明した。最初は「当日モードを書き換えて、現在時刻スタートにすればいい」と考えたが、東郷さんから「乗ったアトラクションをどう扱うか」という新論点が来て、結局「当日モード = リアルタイム再生成」という独立した役割に再定義する流れになった。

結果、シミュ = 前日叩き台 / 当日 = 当日インクリメンタル運用、と役割が綺麗に分離。コード上も `is_sim_mode` 分岐で UI を出し分けるだけで、router/predictor は無触り。後方互換も完全維持（`generate_route()` の `visited` パラメータをデフォルト None に）。

**学び**：複数のモード・機能で重複を感じたら、「片方を削る」より「役割を明確に分けて両立させる」方が、結果的にコードもメンタルモデルもクリーンになる。特に Streamlit のような単一ページの UI では、`mode` radio で分岐させて UI を出し分けるパターンが効く。

---

### 17. 「データ蓄積 vs 合成シミュレーション」の ROI で迷ったら、まず目的を言語化させる
東郷さんから「前日にプランニングできるよう、過去数日分の混雑データから最適ルートを生成できないか」という要望が出た時、技術的に成立する案（GitHub Actions cron で snapshot 蓄積 + 履歴平均化）に飛び付かず、**「目的は何か」を先に切り分けた**のが正解だった。

選択肢を 2 つ提示：
- **案 A**: 既存 `predict_wait()` の理論値だけで合成 snapshot を作るシミュレーションモード（半日実装）
- **案 B**: 履歴データ蓄積 + 平均化（インフラ整備 1.5〜2 日）

東郷さんは「**心の準備・叩き台**」が目的と明言。これで案 B は過剰投資と判断できた。来園日まで 4 日では月曜サンプルゼロ、平日平均 3-4 日では予測精度向上効果は体感できない。実データに見える形でも、結局 predictor の理論値が支配的。

**学び**：実装難度が大きく異なる選択肢が並んだら、必ず **「目的は何か」「精度と速度のどちらを優先か」を東郷さんに先に決めてもらう**。技術的興味で重い案に流されない。CLAUDE.md §4「精度向上のための過剰実装は入れない」を ROI 判断のアンカーに使う。

---

### 11. 範囲チェックは緩く設定し、距離チェックで補完する
当初の TDL 緯度経度範囲チェック（lng 139.85〜139.91）は omnibus の誤入力（139.891）を**通してしまった**。物理的範囲を厳密化する代わりに、「**エントランスからの距離が他の点と比べて突出していないか**」を加える方が頑健。点単体の範囲ではなく、点の集合としての分布で異常を見る視点。

**学び**：範囲制約は false negative（誤検出見逃し）を許容するくらい緩く、代わりに **分布ベースの sanity check** を別途実装する。
