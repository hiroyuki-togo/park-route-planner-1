# TDL Route Planner — Project Memory

> Claude Code がプロジェクト文脈を素早く把握するためのメモリファイル。
> 関連ファイル：[docs/superpowers/specs/2026-05-16-tdl-route-planner-design.md](docs/superpowers/specs/2026-05-16-tdl-route-planner-design.md)（実装仕様書、正本）

---

## 1. プロジェクト概要

**目的**：東京ディズニーランド（TDL）来園日に使う個人ツール。当日のアトラクション別リアルタイム待ち時間を取得し、終日ルートを自動生成する。

**オーナー**：東郷（個人利用ツール／非商用）

**ステータス**：設計確定、Phase 1 実装着手前

**来園日**：**2026-05-25（月曜）**

**残り日数**：本ファイル更新時点で 9 日

---

## 2. 確定した要件

### 2.1 スコープ

| 項目 | 内容 |
|---|---|
| 用途 | 個人ツール（公開・商用化なし） |
| パーク | 東京ディズニーランド（**TDL のみ**。シーは対象外） |
| 来園条件 | 特別イベントなしの平日 |
| 主機能 | 終日ルートの自動生成 |
| 運用 | 当日中に 2〜3 回手動再生成（朝・昼前・夕方） |
| DPA | **使用前提**（コアアトラクションは DPA で乗る） |
| 同行者 | あり想定。URL 共有による閲覧のみ。設定編集は東郷さん代表 |
| 雨天対応 | あり（UI トグル、移動係数 1.7、屋外アトラクション優先度ダウン） |

### 2.2 含めない機能（v1）

- リアルタイム動的最適化
- DPA 購入候補の最適化提案
- 過去データからの機械学習予測
- シー対応・複数日対応
- 当日履歴の永続蓄積（Streamlit Cloud 制約のため）
- 動的天気取得 API
- 認証・同行者との設定同期

### 2.3 技術スタック

- **言語**：Python 3.11+
- **UI**：Streamlit
- **データ取得**：requests + BeautifulSoup
- **計算**：geopy（距離）、pandas（データ整形）
- **永続化**：streamlit-local-storage（日付スコープ）
- **デプロイ**：Streamlit Community Cloud
- **DB**：使わない（ローカル JSON で完結）

Next.js + Supabase は採用しなかった（個人ツールで 1 日しか使わないのに過剰）。

---

## 3. 意思決定の記録

### 3.1 初期相談（TDS 想定）

東郷さんから「ディズニーシーの公式アプリが公表している当日のリアルタイム混雑データを取得して、最適なルートを構築するアプリ」を作りたいとの相談。当初は TDS 向けに設計を進めた（[archive/ディズニープラン-TDS.md](archive/ディズニープラン-TDS.md)）。

### 3.2 データ取得経路の調査

公式 API が存在しないことを確認。3 つの選択肢から **公式サイト（tokyodisneyresort.jp）の HTML スクレイピング** を採用。理由：

- 一次情報で鮮度・正確性が高い
- 規約リスクが最小（過度な頻度を避ければ慣行上許容）
- 実装が単純

却下した選択肢：

- サードパーティ集約サイト → 二次情報のため
- 公式アプリ API 解析 → MyDisney ログイン必須・規約違反濃厚

### 3.3 スコープ確定

- 用途：個人ツール
- 来園日まで 1 週間以内（当初）→ 5/25 確定で 9 日に
- 重視機能：終日ルートの自動生成
- 「朝に1回計算→終了」ではなく **「当日中に 2〜3 回再生成」** 運用

### 3.4 来園パーク変更（2026-05-16）

**TDS → TDL に変更**。理由：実際に行く予定がランドに変わったため。設計の根幹（アーキテクチャ、DPA 対応、must-visit、localStorage 永続化、テスト方針）はそのまま流用可能。差分は以下：

- スクレイピング URL：`/tds/...` → `/tdl/...`
- マスタデータ：TDS アトラクション → TDL アトラクション（25 件、座標再測定）
- エリア構成：TDS 8 エリア → TDL 7 エリア
- `AREA_MIN_TIME`：水路分断のない TDL では必要性大幅減
- **新規論点：パレード横断ペナルティ**（TDS にはない制約）
- `requires_reservation` 対象：ファンタジースプリングス 4 施設 → 美女と野獣（要確認）

### 3.5 設計上の重要判断（TDL 確定後）

1. **DPA 対応**：`fixed_block` の type に `dpa` を追加。`attraction_id` + 時間窓を持ち、待ち時間 15 分固定でルートに組み込む
2. **must-visit ハイブリッド**：「必ず乗る」チェックボックス + ★1-5 スライダー。必ず乗る組が残っていれば候補プールをそれだけに絞る
3. **localStorage 永続化（日付キー）**：`tdl_settings_{YYYY-MM-DD}`。当日中は自動復元、日付が変わるとクリーンスタート
4. **パレード対応**：`fixed_block` の type に `parade` を追加。`watch=True` なら鑑賞、`False` ならメインストリート横断移動に +15 分ペナルティ
5. **雨天モード**：UI トグル。`park_factor` 1.4 → 1.7、屋外アトラクション `experience_value` × 0.7、屋外予測待ち -30% / 屋内 +20%
6. **レストランマスタ**：主要 10 店舗（TS 5-6 + CS 4-5）を `restaurants.json` 化。食事ブロックでレストラン選択 → 所要時間・座標を自動セット
7. **固定ブロック衝突**：計画時点で「次の固定ブロックまでに収まらない候補」を弾く（F 処理）
8. **同行者**：URL 共有による閲覧のみ。協調編集は v2 以降

---

## 4. Claude Code で作業する際の指示

### 4.1 進め方

仕様書 §10 の **Phase 1 → 7 の順** で進める。各フェーズに「Claude Code 指示テンプレート」を作成する想定（次フェーズ：writing-plans）。

### 4.2 スタイルガイド

- **同士スタンス**：懸念は主体的に指摘してほしい
- **両論併記**：実装判断で迷ったら、選択肢とトレードオフを提示してから進める
- **PREP 構成**：報告・提案は結論先出し
- **絵文字**：視認性補助のみ（⚠️ ✅）

### 4.3 注意事項

- ⚠️ 個人利用に限定（公開・商用化なし）
- ⚠️ GitHub 公開する場合は「個人学習目的、商用利用不可」明示、Disney/OLC 商標は使わない
- ⚠️ スクレイピング頻度は 5 分に 1 回が下限
- ⚠️ アトラクション名の表記揺れに注意（公式サイトと `scrape_key` のファジーマッチが必要）
- ⚠️ スペースマウンテン・スプラッシュマウンテンは閉鎖中、マスタから除外

---

## 5. 当日の運用フロー

```
朝 8:00 自宅
  → URL を開く（localStorage から前日設定 or 初期化）
  → 優先度・食事時間・パレード時刻を設定

朝 9:00 開園
  → 公式アプリで DPA 購入したらアプリに入力
  → 「更新」→「ルート生成」
  → 推奨ルートに従って最初のアトラクションへ

11:00 ごろ
  → 待ち時間ズレ補正のため「更新」→「ルート生成」
  → 雨が降り始めたら雨天モード ON

12:30 昼食後
  → 同様に再生成

16:00-17:00
  → 残り時間でのルート再生成

20:00 以降
  → パレード鑑賞 or 夜景アトラクション
```

---

## 6. 関連ファイル

- [docs/superpowers/specs/2026-05-16-tdl-route-planner-design.md](docs/superpowers/specs/2026-05-16-tdl-route-planner-design.md)：実装仕様書（正本）
- [CLAUDE.md](CLAUDE.md)：Claude Code 向けプロジェクト指示
- [archive/ディズニープラン-TDS.md](archive/ディズニープラン-TDS.md)：TDS 向け旧版（参考のみ）
- `data/attractions.json`：アトラクションマスタ（Phase 3 で作成）
- `data/restaurants.json`：レストランマスタ（Phase 3 で作成）
- `data/snapshots/`：取得した待ち時間履歴（Phase 2 で自動生成、揮発）

---

## 7. 将来拡張メモ（v2 以降）

1. 当日取得したスナップショットを外部ストレージに永続化
2. 複数来園分のデータが溜まったら、時間帯別予測モデルを学習
3. DPA 取得時間の `fixed_block` 自動投入
4. シー対応・複数日対応
5. PWA 化、オフライン対応
6. TDS 向け設計（archive/ にある）を統合し、`park` 引数で切り替え可能な TDR Route Planner 化
